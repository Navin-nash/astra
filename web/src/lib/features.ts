/**
 * Feature flags — flip BETA_MODE to switch between waitlist and live access.
 *
 * true  → waitlist / beta mode  (shows "Join waitlist" / "Get early access")
 * false → live mode             (shows "Get started" / "Sign in")
 */
export const BETA_MODE = true;
